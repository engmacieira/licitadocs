# 📦 Log de Atualização: Fundação do Core e Segurança

**Data:** 17/01/2026
**Módulo Afetado:** Core / Backend
**Arquivos Principais:**
* `app/core/database.py`
* `app/core/security.py`
* `app/models/user_model.py`
* `app/repositories/user_repository.py`
* `app/schemas/user_schemas.py`

## 📝 Resumo da Mudança
Implementação da infraestrutura base para persistência de dados e segurança. Foi configurada a conexão com o banco de dados (SQLAlchemy), criadas as tabelas de Usuário e Empresa com suporte a UUID, e implementado o sistema de hash de senhas para garantir que nenhuma credencial seja salva em texto puro.

## 🔧 Detalhes Técnicos (Para a IA)
* **Banco de Dados:** Criação das tabelas `users` e `companies`. Configuração do engine SQLAlchemy com suporte a SQLite (dev) e preparado para Postgres (prod).
* **Lógica:** Implementação do padrão Repository (`user_repository.py`) para abstrair operações de banco e tratar erros de integridade (rollback).
* **Dependências:** Adicionado `passlib[bcrypt]` para criptografia e `pydantic[email]` para validação de schemas.

## ⚠️ Pontos de Atenção
* O arquivo `.env` deve estar configurado corretamente com a `DB_URL`.
* As senhas já estão sendo hashadas automaticamente no repository antes de salvar. **Nunca** inserir usuários manualmente no banco via SQL sem gerar o hash antes.

## 💻 Comando de Commit Sugerido
```bash
git add .
git commit -m "feat(core): implementa fundação de persistência e segurança" -m "- Cria docs/v0.1.0_SETUP_CORE.md com log da versão" -m "- Adiciona Security Hash, Schemas e User Repository" -m "- Configura Tabelas User e Company com UUID"