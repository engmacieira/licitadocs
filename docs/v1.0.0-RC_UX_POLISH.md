# 📦 Versão v1.0.0-RC: UX, Polimento Visual & Responsividade

**Data:** 03/02/2026
**Tag:** `v1.0.0-RC` (Release Candidate)
**Codinome:** The Visual Update

## 🎯 Visão Geral
Esta versão transforma o **LicitaDoc** de um MVP funcional em um produto polido e agradável. O foco total foi na **Experiência do Usuário (UX)** e **Design de Interface (UI)**. Eliminamos comportamentos de "protótipo" (telas brancas, alertas nativos, layouts quebrados no mobile) e introduzimos feedbacks visuais modernos, carregamento suave e uma arquitetura de componentes reutilizáveis.

---

## 🎨 Principais Mudanças (Changelog)

### ✨ Frontend (UX & Interação)
* **Sistema de Notificações (Toasts):** Substituição total dos `window.alert()` e `console.error()` pela biblioteca **Sonner**.
    * Feedback verde/vermelho elegante para ações de Login, Upload e Exclusão.
    * Notificações automáticas via interceptor no `api.ts` para erros de conexão (500) e sessão (401).
* **Carregamento Suave (Skeletons):** Implementação de "esqueletos" de carregamento nas tabelas, cards e chat. O layout não "pula" mais enquanto os dados chegam da API.
* **Upload com Progresso:** O envio de arquivos agora exibe um Toast de "Enviando..." que se resolve automaticamente ao finalizar, prevenindo cliques duplos.
* **Responsividade Mobile:** A Sidebar agora se esconde em telas pequenas e pode ser acessada via menu hambúrguer. As tabelas ocultam colunas secundárias automaticamente no celular.

### 🧱 Componentização (Design System)
* **Biblioteca UI Própria:** Criação/Refatoração de componentes base em `src/components/ui/`:
    * `<Input />`: Suporte a ícones internos (`lucide-react`) e mensagens de erro/ajuda.
    * `<Button />`: Variantes visuais (Primary, Ghost, Danger) e estado de `isLoading` integrado (spinner).
    * `<StatsCard />`: Cards de métricas padronizados com suporte a temas de cores (Verde/Azul/Âmbar).
* **Chat Global:** O assistente de IA deixou de ser uma página isolada e virou o `<ChatWidget />`, um componente flutuante acessível de qualquer tela do sistema.

### 🧹 Limpeza & Refatoração (Clean Code)
* **Remoção de Legado:** A pasta `src/pages/AIChat` foi deletada, simplificando as rotas.
* **Hardening de Auth:** O `AuthContext` agora decodifica o JWT no boot da aplicação para verificar a data de expiração (`exp`), forçando logout imediato se o token for antigo.
* **Modernização CSS:** Atualização do `index.css` para a sintaxe do **Tailwind CSS v4** (`@import "tailwindcss";`).

---

## 🔧 Detalhes Técnicos (Para Devs)

* **Novas Dependências:**
    * `sonner`: Gerenciamento de Toasts.
    * `clsx` & `tailwind-merge`: Utilitários para criação de componentes CSS dinâmicos.
* **Configuração VS Code:** Adicionado `.vscode/settings.json` para evitar falsos positivos de linting com a sintaxe `@apply` do Tailwind.
* **Build:** O arquivo `App.css` padrão do Vite foi limpo para evitar conflitos de layout (centralização forçada).

---

## ⚠️ Notas de Manutenção
* **Rotas Alteradas:** A rota `/ai-chat` não existe mais. O acesso à IA é exclusivamente pelo widget flutuante.
* **Segurança de Upload:** O Frontend agora valida o tipo de arquivo (PDF) antes mesmo de enviar a requisição, economizando banda.

---

## 📊 Status do Projeto
* **Backend:** Estável (v0.9.0)
* **Frontend:** Polido (v1.0.0-RC)
* **Próximo Passo:** Sprint 12 (Infraestrutura, Dockerização e Deploy).