# 📦 Log de Versão: v0.6.0 - Corporate Management Module

**Data:** 29/01/2026
**Módulo Afetado:** Admin / Core
**Status:** ✅ Estável

## 📝 Resumo da Versão
A versão **v0.6.0** introduz o **Módulo Administrativo**. O sistema agora possui capacidade multi-inquilino (multi-tenant) básica, permitindo o cadastro e gestão de diferentes empresas clientes.

## 🚀 Novas Funcionalidades

### 🏢 Gestão de Empresas (CRUD)
* **Cadastro:** Permite registrar Razão Social e CNPJ (com validação de 14 dígitos).
* **Listagem:** Visão geral de todas as empresas cadastradas com busca rápida.
* **Edição:** Permite corrigir o nome da empresa (CNPJ é travado por segurança).
* **Exclusão:** Permite remover empresas (Soft ou Hard delete via API).

### 🛡️ Segurança & Qualidade
* **Validação de Schema:** O Backend rejeita payloads com campos extras ou tipos errados automaticamente.
* **Proteção de Rota:** Apenas usuários com `role='admin'` acessam as rotas `/admin/*`.

## 🔧 Alterações Técnicas
* Adicionado `react-hook-form` e `zod` ao `package.json` do Frontend.
* Criado `app/tests/test_companies.py` para garantia de qualidade do backend.
* Ajuste no `company_repository.py` para mapeamento correto de colunas (`name` -> `razao_social`).

## 💻 Como Rodar
1. Backend: `pytest` (para garantir integridade) -> `uvicorn app.main:app --reload`.
2. Frontend: `npm install` (novas deps) -> `npm run dev`.