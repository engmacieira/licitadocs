# 📦 Log de Versão: v0.5.1 - Frontend Foundation & Features (Stable)

**Data:** 21/01/2026
**Módulo Afetado:** Frontend (React) / Backend (FastAPI Router)
**Status:** ✅ Estável / Em Produção (Local)

## 📝 Resumo da Versão
Esta versão marca a conclusão da **Fundação do Frontend**. O sistema deixou de ser apenas uma API e agora possui uma interface gráfica totalmente funcional. Inclui autenticação robusta, gestão de documentos conectada e o primeiro módulo de Inteligência Artificial interativo.

Também corrige problemas críticos de **Rede e Roteamento** (Erro 304, Proxy Vite e Trailing Slashes) identificados durante a integração.

## 🚀 Novas Funcionalidades (Features)

### 1. Gestão de Documentos (Full)
* **Listagem Conectada:** Tela "Meus Documentos" consome a API real (`GET /documents/`).
* **Upload de PDF:** Implementado envio de arquivos (`POST /documents/upload/`) com feedback visual (loading/sucesso).
* **Tratamento de Erros:** Mensagens amigáveis para falhas de conexão ou listas vazias.

### 2. Consultor IA (Chat)
* **Interface de Chat:** Nova tela `/ai-chat` com layout estilo mensageiro.
* **Integração Gemini:** O Frontend envia perguntas para o Backend, que consulta o catálogo e retorna respostas contextualizadas.
* **UX:** Indicadores de "Digitando..." e rolagem automática para novas mensagens.

### 3. Infraestrutura & Segurança
* **AuthContext:** Persistência de sessão (JWT) e proteção de rotas privadas (`ProtectedRoute`).
* **Axios Interceptor:** Injeção automática do Token Bearer em todas as requisições.

---

## 🐛 Correções e Ajustes Técnicos (Fixes)

* **[CRÍTICO] Roteamento Duplicado:** Removida rota placeholder no `App.tsx` que impedia o carregamento da página real de documentos.
* **[CRÍTICO] Trailing Slash (Barra Final):** Ajustadas todas as chamadas de API para incluir a barra final (`/`), evitando o *Redirect 307* do FastAPI que causava perda do cabeçalho de Autorização (Erro 401).
* **[Rede] Conexão Direta:** Configurado endereço explícito no `documentService.ts` para contornar problemas intermitentes de resolução de DNS/Proxy no ambiente de desenvolvimento local (Registrado como Dívida Técnica).

---

## 🔧 Artefatos Modificados
* `frontend/src/App.tsx` (Limpeza de rotas)
* `frontend/src/services/documentService.ts` (Implementação final)
* `frontend/src/services/aiService.ts` (Novo)
* `frontend/src/pages/Documents/index.tsx` (Lógica de tela)
* `frontend/src/pages/AIChat/index.tsx` (Novo componente)

## 💻 Próximos Passos (Roadmap v0.6.0)
* **Gestão de Empresas:** CRUD completo para cadastro de empresas licitantes.
* **Refinamento de Upload:** Adicionar campos de data de validade e categoria.