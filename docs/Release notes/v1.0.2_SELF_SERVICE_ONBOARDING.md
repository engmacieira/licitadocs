# 📦 Release Notes: v1.0.2 - Self-Service Onboarding

**Data:** 05/02/2026
**Sprint de Origem:** Sprint 13
**Responsável:** Matheus & Mark

---

## 📝 Resumo Executivo
Esta versão introduz o fluxo completo de **Onboarding de Clientes (SaaS)**. Agora, novas empresas podem se cadastrar publicamente através da Landing Page, enviar seus documentos constitutivos (Contrato Social/CNPJ), assinar termos digitalmente e realizar o pagamento (simulado) para liberar o acesso ao sistema.

O foco foi transformar o LicitaDocs em uma plataforma "Self-Service", removendo a necessidade de cadastro manual por um administrador.

## ✨ Changelog

### 🚀 Novas Funcionalidades (Feat)
* **[Frontend] Landing Page:** Nova página inicial pública (`/`) com apresentação do produto e CTA para cadastro.
* **[Frontend] Cadastro Wizard:** Formulário de registro em etapas (Dados Cadastrais -> Upload de Documentos).
* **[Frontend] Formalização:** Tela de assinatura digital de "Contrato de Adesão" e "Procuração".
* **[Frontend] Checkout:** Tela de pagamento simulada que desbloqueia o acesso do usuário.
* **[Backend] Upload Multipart:** Novo suporte no endpoint `/auth/register` para receber JSON e Arquivos (`multipart/form-data`) na mesma requisição.
* **[Backend] Storage Local:** Implementação do utilitário `file_helper.py` para salvar arquivos na pasta `storage/uploads`.

### 🏗️ Melhorias Técnicas (Refactor)
* **[Auth] Regras de Negócio:**
    * Novos usuários são criados automaticamente com a role `CLIENT` (Cliente).
    * Novos usuários nascem com status `is_active=False` (Inativo) até a confirmação do pagamento.
* **[Database] Mapeamento de DTOs:** Correção na camada de Controller para traduzir campos do Frontend (Inglês) para o Schema do Banco (Português/Legacy).
* **[API] Simulação de Pagamento:** Endpoint `/auth/simulate-payment` criado para testes de ativação de conta.

---

## 🔧 Detalhes Técnicos Críticos (Para Devs)

### 1. Sistema de Arquivos (Storage)
Esta versão cria e depende da pasta local `storage/uploads/` na raiz do projeto backend.
* **Atenção:** Certifique-se de que o usuário do S.O. ou Container tem permissão de escrita nesta pasta.
* *Nota:* Em ambientes de produção (Docker), volumes persistentes devem ser configurados para não perder esses dados.

### 2. Endpoints Alterados
* `POST /auth/register`: Agora exige `multipart/form-data`. O envio de JSON puro (application/json) falhará.
* `POST /auth/login`: Agora bloqueia usuários com `is_active=False` (retorna 403 Forbidden).

### 🐛 Known Issues (Problemas Conhecidos)
* **Hardcode de Tradução:** O mapeamento de campos (`legal_name` -> `razao_social`) é feito manualmente no Router.
* **Segurança:** A rota de simulação de pagamento é pública e deve ser removida ou protegida antes do deploy em produção real.