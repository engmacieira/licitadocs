# 📜 Changelog v0.5.0: Frontend Foundation

**Data:** 18/01/2026
**Versão:** 0.5.0
**Foco:** Criação da Interface do Usuário (React) e Integração Básica.

---

## 🚀 Novas Funcionalidades (Frontend)

### 🏗️ Infraestrutura & Core
* **Inicialização do Projeto:** Setup com **Vite + React + TypeScript**.
* **Estilização:** Configuração do **Tailwind CSS** (v4/PostCSS) e biblioteca de ícones **Lucide React**.
* **Networking:** Configuração do **Axios** com interceptors automáticos para injeção de Token JWT.
* **Proxy Reverso:** Configuração do `vite.config.ts` para redirecionar chamadas `/api` para o Backend (`localhost:8000`), resolvendo problemas de CORS.

### 🔐 Autenticação e Segurança
* **AuthContext:** Sistema global de gerenciamento de sessão (Login/Logout).
* **Persistência:** Armazenamento seguro do Token no `localStorage`.
* **Rotas Protegidas:** Componente `<ProtectedRoute>` que impede acesso de usuários não logados.
* **Tela de Login:** Interface completa com validação visual e integração com endpoint `POST /auth/login`.

### 🖥️ Interface do Usuário (UI)
* **Design System:** Criação de componentes reutilizáveis (`Button`, `Input`, `StatsCard`).
* **App Shell:** Layout principal com **Sidebar Lateral** fixa e Header dinâmico.
* **Dashboard:** Tela inicial com indicadores de performance (Cards).
* **Meus Documentos:** Tela de listagem com tabela zebrada, conectada ao endpoint `GET /documents`.

---

## 🛠️ Ajustes no Backend
* **CORS:** Habilitação do Middleware de CORS no FastAPI (`main.py`) para permitir conexões locais.
* **Hotfix:** Ajustes de compatibilidade para receber requisições do Proxy do Vite.

---

## 📉 Dívidas Técnicas (Conhecidas)
* **Feedback de Erro:** Melhorar mensagens de erro (Toasts) para falhas de conexão.
* **Testes de Frontend:** Implementar testes unitários para componentes React.
* **Validação de Formulário:** Adicionar Zod/React Hook Form para validações mais robustas.

---

## 📊 Estatísticas
* **Novos Arquivos:** +15 arquivos (.tsx, .ts, .css)
* **Componentes Criados:** 6
* **Rotas Implementadas:** `/`, `/dashboard`, `/documents`