# 🔖 Release Notes: v0.4.0 - AI Consultant

**Data:** 17/01/2026
**Versão Anterior:** v0.3.0 (Admin & Concierge)
**Status:** Estável (Backend + AI)

---

## 📢 Resumo da Versão
Esta versão introduz a camada de **Inteligência Artificial** ao ecossistema LicitaDoc.
Integramos o modelo **Google Gemini** para atuar como um "Consultor Jurídico Virtual". O sistema agora é capaz de interpretar dúvidas de usuários sobre editais e correlacioná-las com o catálogo de documentos da plataforma.

Também realizamos modernizações estruturais nos Schemas de dados (Pydantic V2).

---

## ✨ Novas Funcionalidades (Changelog)

### 🧠 Inteligência Artificial (Gemini)
* **Chatbot Consultivo:** Novo endpoint `POST /ai/chat`.
    * O usuário envia uma dúvida (ex: "O que é prova de regularidade fiscal?").
    * A IA analisa o contexto e responde indicando qual documento do sistema atende àquela exigência.
* **Geração de Texto:** Capacidade de redigir declarações simples (ex: "Declaração de Menores") sob demanda.
* **Contexto Injetado (RAG):** Implementação de um catálogo (`catalog.py`) que é enviado ao prompt do modelo, garantindo que a IA conheça os produtos da LicitaDoc.

### 🛠️ Engenharia de Software
* **Mocking de Testes:** A suíte de testes agora simula as respostas do Google. Isso permite rodar testes de IA sem consumir créditos da API e sem latência de rede.
* **Refatoração Pydantic:** Atualização de todos os `BaseModels` para usar `ConfigDict` (padrão V2), eliminando warnings de depreciação.

---

## 🔧 Configuração Necessária
* **Variáveis de Ambiente:** É obrigatório configurar `GOOGLE_API_KEY` no arquivo `.env` para que o módulo de IA funcione.

## 🧪 Qualidade (QA)
* **Cobertura:** 15 Testes Automatizados (100% Passing).
* **Segurança:** A rota de IA é protegida e exige autenticação (`Bearer Token`).

---

## 🔮 Próximos Passos
* Construção do **Frontend** (Interface Visual).
* Deploy em Nuvem (Render/Railway/AWS).